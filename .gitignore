# Dependencias de Node
node_modules/
package-lock.json

# Variables de entorno y secretos
.env

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# ROMs de juegos (Protección de Copyright)
# Ignoramos todos los archivos dentro de roms, pero mantenemos la carpeta
public/roms/*
!public/roms/.gitkeep